############################################################
# Production Environment Example (Vite)
# Copy or adapt to .env.production
# Only define VITE_API_URL (primary); others are legacy fallbacks
############################################################

# Primary backend origin (no trailing slash). Client adds /api/
VITE_API_URL=https://api.bairengroup.com

# Legacy fallbacks (do NOT set if the above is present)
# VITE_API_BASE_URL=https://api.bairengroup.com
# VITE_API_BASE=https://api.bairengroup.com

# If switching to cookie-based auth later, backend must allow this origin via CORS/CSRF.
# Example CORS_ALLOWED_ORIGINS entry: https://www.bairengroup.com