############################################################
# Production Environment Example (Vite)
# Copy or adapt to .env.production
# Only define VITE_API_URL (primary); others are legacy fallbacks
############################################################

# Preferred new variables (base + prefix)
VITE_API_BASE=https://api.bairengroup.com
VITE_API_PREFIX=/api

# Legacy single var (full path including /api). If you use this, omit the two above.
# VITE_API_URL=https://api.bairengroup.com/api

# Older transitional names (avoid introducing in new envs):
# VITE_API_BASE_URL=https://api.bairengroup.com/api

# NOTE: No trailing slash anywhere; the client ensures exactly one.

# If switching to cookie-based auth later, backend must allow this origin via CORS/CSRF.
# Example CORS_ALLOWED_ORIGINS entry: https://www.bairengroup.com