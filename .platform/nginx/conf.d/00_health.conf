# Fast path health responses served directly by Nginx before proxying to Gunicorn
# Both /health and /health/ return 200 plain text to satisfy ALB/ELB and external monitors.

location = /health { return 200 "ok"; add_header Content-Type text/plain; }
location = /health/ { return 200 "ok"; add_header Content-Type text/plain; }
